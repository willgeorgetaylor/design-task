import { FakeOptions } from './core/core';
/**
 * Generate a random account.
 *
 * @category finance
 *
 * @example
 *
 * randAccount()
 *
 * @example
 *
 * randAccount({ length: 10 })
 *
 */
export declare function randAccount<Options extends FakeOptions = never>(options?: Options): [Options] extends [never] ? string : Options["length"] extends number ? string[] : string;
