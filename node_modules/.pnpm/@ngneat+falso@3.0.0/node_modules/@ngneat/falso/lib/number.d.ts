import { FakeOptions } from './core/core';
export interface RandomNumberOptions extends FakeOptions {
    min?: number;
    max?: number;
}
/**
 * Generate a random number.
 *
 * @category general, math
 *
 * @example
 *
 * randNumber()
 *
 * @example
 *
 * randNumber({ length: 10 })
 *
 * @example
 *
 * randNumber({ min: 10, max: 1000 }) // default is 'min': 0, 'max': 999_999
 *
 */
export declare function randNumber<Options extends RandomNumberOptions = never>(options?: Options): [Options] extends [never] ? number : Options["length"] extends number ? number[] : number;
