import { FakeOptions } from './core/core';
/**
 * Generate a random ibn.
 *
 * @category finance
 *
 * @example
 *
 * randIban()
 *
 * @example
 *
 * randIban({ length: 10 })
 *
 */
export declare function randIban<Options extends FakeOptions = never>(options?: Options): [Options] extends [never] ? string : Options["length"] extends number ? string[] : string;
