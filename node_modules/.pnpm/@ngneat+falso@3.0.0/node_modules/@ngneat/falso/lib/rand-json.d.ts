import { FakeOptions } from './core/core';
export interface RandomJSONOptions extends FakeOptions {
    min?: number;
    max?: number;
}
/**
 * Generate a random JSON Object.
 *
 * @category Datatype
 *
 * @example
 * randJSON()
 *
 * @example If a fixed number of keys are required
 *
 * randJSON({ length: 10 })
 *
 * @example If a random number of keys are required
 *
 * randJSON({ min: 1, max: 10 })
 */
export declare function randJSON<Options extends RandomJSONOptions = never>(options?: Options): {
    [key: string]: any;
};
